import React from 'react'
import Head from '../components/Head'

export default class Main extends React.component {
  render() {
    return (
      <html>
        <Head title='React and CSS Kibbles and Nibbles' />
        <body>
          {/* This is where the pages go! */}
        </body>
      </html>
    )
  }
}